<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/btn_remove_filter"
            android:layout_width="match_parent"
            android:layout_height="@dimen/btn_remove_filter_height"
            android:background="@color/remove_filter_background"
            android:gravity="center"
            android:text="@string/string_remove_filter"
            android:visibility="invisible" />

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>
    </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_add"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="@dimen/margin_standart"
        android:layout_marginEnd="@dimen/margin_standart"
        android:src="@android:drawable/ic_menu_add" />

</FrameLayout>
